services:
  - type: web
    name: cipher-messaging-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: API_KEY
        generateValue: true
      - key: DATABASE_URL
        value: postgresql://postgres.gebanlqunirbitsbyypf:TFNAtGdWWDwKGB0S@aws-0-us-west-2.pooler.supabase.com:6543/postgres
